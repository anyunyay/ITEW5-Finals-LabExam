{
  "name": "sports-pwa-task-manager-root",
  "version": "1.0.0",
  "description": "Sports PWA Task Manager - Root workspace",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "install:all": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "concurrently": "^8.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "mongodb": "^7.0.0",
    "sharp": "^0.34.5",
    "socket.io-client": "^4.8.1"
  }
}
